/**
 * Supabase server client — usar en Server Components y Route Handlers.
 *
 * Integración (paso 5 del plan):
 * - createServerClient de @supabase/ssr con cookies de Next
 * - Queries siempre con .select() explícito de columnas, nunca select('*')
 */
export {};
